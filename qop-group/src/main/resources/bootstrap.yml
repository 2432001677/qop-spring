spring:
  application:
    name: qop-group
  cloud:
    nacos:
      server-addr: 106.53.136.182:8848
      discovery:
        namespace: ${spring.cloud.nacos.config.namespace}
      config:
        file-extension: yaml
        namespace: @profileActive@
